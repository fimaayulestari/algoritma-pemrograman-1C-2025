# ===NO1===
# #membuat program u/ menampilkan bilangan prima dari 1 sampai n

# Input nilai n
n = int(input("Masukkan nilai n: "))
print(f"Bilangan prima dari 1 sampai {n} adalah:")

# Loop dari 1 sampai n
for angka in range(1, n):
    if angka > 1:     # bilangan prima lebih besar dari 1
        prima = True  # asumsikan angka adalah prima
                      # cek pembagi dari 2 sampai angka-1
        for i in range(2, angka):
            if angka % i == 0:
                prima = False  # angka bukan prima karena habis dibagi i
                break
        if prima:
            print(angka, end=" ")
print()